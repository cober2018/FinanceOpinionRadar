## 1. 确认范围与标注协议

- [x] 1.1 确认用户已明确批准本 proposal（含建议语义精确率 98% 门槛）；使用 executing-plans 在 task_plan.md 写出 `///` 实施步骤后才进入 apply。
- [ ] 1.2 更新 `tests/golden/README.md` 为可执行标注手册，写清字段定义、正反例、证据定位、资格状态、完整性、人工裁决和音视频核验边界。
- [ ] 1.3 在 `tests/golden/sources.json` 和现有 annotations 中保留原来源及内容，将现有两条样例明确标为 demo；定义输入、预测和裁决所需字段，不伪造人工签署。
- [ ] 1.4 记录正式验收覆盖清单、开发/独立验收按来源分组规则及实际缺口；有真实本地材料则整理 draft，没有则明确缺失，完整人审数据收集列为后续业务工作。

## 2. 冻结输入与运行可复现性

- [ ] 2.1 改造 `scripts/evaluate_viewpoints.py`，从本地文件读取冻结转录、预测和裁决；离线模式移除 DB 初始化与外部模型调用。
- [ ] 2.2 增加显式生成预测入口，复用既有提示词、provider 和全文/分块策略，处理完整输入；记录实际模型/提示词/模式与输入哈希，保存原始输出，不调用生产落库流程。
- [ ] 2.3 增加 schema、来源、文件哈希、段落归属、摘录与时间范围校验；重复 ID、冲突对齐和过期裁决明确失败。
- [ ] 2.4 保存逻辑单元计划和最多三次尝试记录，按首次尝试统计结构通过率；对最终调用失败、部分处理、结构失败和成功无观点分别记账，不覆盖历史运行。

## 3. 人工裁决、评分与验收

- [ ] 3.1 生成逐条待审材料及对齐候选；正式评分使用人工确认的一对一匹配，不以方向、期限或实体正确为匹配前提。
- [ ] 3.2 按 spec 实现结构通过、证据缺失、审核覆盖、语义精确、完整标注召回及分字段准确率，保留失败和重复项；零分母为不适用。
- [ ] 3.3 更新 `tests/golden/thresholds.json`，与 PRD 既有门槛及已确认的新语义精确率门槛一致；默认执行资格及阈值校验，baseline 只作兼容口径对比。
- [ ] 3.4 输出 JSON/Markdown 报告，含版本、阈值、分子分母、来源分组、逐条差异、实际覆盖、待审数及 quality_status；落实主状态优先级与退出码约定，并列出全部阻碍原因。

## 4. 验证与审阅

- [ ] 4.1 使用现有真实样例验证演示流程可运行、正式评测因未完成的人审及覆盖要求而明确未验收；记录命令、退出码和报告路径。
- [ ] 4.2 对真实样例的临时副本做明确标记的输入扰动，人工检查方向相反、无证据、重复预测、失败素材、pending、零分母和过期裁决的计算结果；不生成测试代码，扰动样本不进入正式基准。
- [ ] 4.3 在数据库不可用且未配置模型凭据的环境验证离线评分；对同一冻结数据重复评分确认计数一致；记录生成预测与线上路径的适用边界。
- [ ] 4.4 运行项目现有、与脚本改动相关的静态检查和必要既有检查，安排 code-reviewer 与 python-reviewer 复核；只修本批问题。

## 5. 文档与规范收尾

- [ ] 5.1 更新根 README、`scripts/README.md`、`tests/golden/README.md` 及三个流程文件，包含真实启动/验证命令、完成项、待审样本缺口和关键决策；同步目标流程图。
- [ ] 5.2 核对本批 spec 已实现、实际验证证据与未完成的业务人审工作，明确“工具完成”与“质量未验收”的状态；完成后执行 OpenSpec archive，不在仅有提案时归档。
