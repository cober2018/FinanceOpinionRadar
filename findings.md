# 配置发现

- 当前工作区：`FinanceOpinionRadar`
- 连接器名称：`Codex with ChatGPT · FinanceOpinionRadar`
- 连接方式：固定域名
- 项目模式：新工作区，需要先由用户创建 ChatGPT 项目合集。
- 验收要求：不能只看连接状态，必须完成工作区身份和项目文件读取验证。

## 2026-09-22 产品矩阵评估发现

- 用户描述：量化平台提供 A 股分析与研发过程素材；AI 内容中心负责选题加工和媒体发布；雷达负责媒体采集、语音转写、观点识别与标注，并考虑拓展其他行业。
- README 声称已具备多源发现、转录、观点抽取、实体归一、规则去重、审核、观点历史与主题共识，以及精华资产和内容摘要留存；这些是文档记录，本轮未验证代码或运行状态。
- README 的 EPIC-04+ TODO 与已完成列表存在重叠，不能直接用 TODO 推断功能缺失。
- 优先澄清：雷达近期首先要改善内容生产、辅助量化研究，还是形成可对外提供的采集标注产品；该选择会改变能力建设顺序。
- 尚不明确：下游实际接入程度、人工处理瓶颈、规模与时效要求、跨行业需求是否已有实际使用者。

### 用户已明确的目标

- 公司内部生产研究使用，三个月优先给 AI 内容中心提供有价值、可加工且有出处的素材。
- 当前主要难点是来源多、数据量大、非结构化内容种类多，清洗和标签识别主要依赖大模型。
- 准确性优先，允许处理较慢；具体规模、人工复核产能和量化质量门槛仍未给出，不作为已确认需求。
- 雷达负责来源采集、忠实抽取、标注、证据和素材交付；内容中心负责最终选题、写作、视频加工与发布；量化平台提供独立分析与验证材料。

### 已核对的代码事实与边界

本次为静态代码核对，不代表生产运行验收。图工具在当前会话未发现可调用能力，采用任务范围内的定向文件检查。

| 项目 | 已有能力 | 待完善边界与证据 |
|---|---|---|
| 清洗 | 字幕优先、ASR 兜底、去空、时间与重叠校验 | 当前 `_clean_segments` 主要做格式和时间校验，不能据此证明数字、专名、否定与条件词转写正确。见 `apps/api/app/services/preparation.py:354`、`apps/api/app/services/media/subtitles.py:31`。 |
| 标签与不确定性 | 主题、实体、方向、期限、条件标志；unclear 人工复核、未知实体候选池 | 当前为财经观点模型；Topic 是平面词典，观点为单 topic/entity，conditional 仅为布尔值。未在核对链路发现领域标签版本、具体条件字段、事实性陈述与外部核验状态的完整区分。见 `apps/api/app/db/models/taxonomy.py:14`、`apps/api/app/db/models/viewpoint.py:42`。 |
| 语义放行 | 结构校验、证据 ID 范围检查、规则审核 | reviewer 依赖模型自报 confidence、证据数量/字数和关键词，未核验原文是否支持观点。不能把 confirmed 直接当成人工核实事实。见 `apps/api/app/services/extraction.py:55`、`apps/api/app/services/reviewer.py:59`。 |
| 讲话人和归属 | 字幕有 speaker_label，观点有关联作者 | 抽取输入未携带 speaker_label，观点作者赋为账号 creator；尚不能证明主持人、嘉宾与被转述者准确分离。见 `apps/api/app/services/extraction.py:137`、`:237`。 |
| 失败与无内容 | 保存 failed_chunks 计数 | LLM 异常后继续，未产出观点时可进入 ready；成功无观点与处理不完整需要分开。见 `apps/api/app/services/extraction.py:178`、`:315`。 |
| 人工修正 | 修改、确认、拒绝及修改前后审计 | PATCH 入口未重新核验证据或调整审核状态；所查范围未见修正回流评测集和下游更正通知。见 `apps/api/app/api/v1/viewpoints.py:228`。 |
| 评测基准 | 已有评估脚本与阈值文件 | sources 只有两条；两份 annotation 均注明“格式示范，正式标注需人工复核”，证据 ID 为空。见 `tests/golden/README.md:12`、`tests/golden/annotations/case_001.json:3`、`tests/golden/annotations/case_002.json:3`。 |
| 评测口径 | 抽取、方向、实体、证据等指标 | 字符相似度不等于语义正确；方向相反可能被排除在方向准确率分母之外；证据指标只查 ID 存在且跳过无证据项。见 `scripts/evaluate_viewpoints.py:37`、`:59`、`:76`。当前阈值与 PRD 质量标准也不一致。 |
| 证据留存 | 原文、起止时间、来源链接可通过现有接口取回；精华资产保留 | 普通内容清理前的快照只保留结论等字段，缺来源链接、原文定位、期限条件；源条目删除会级联删除转录与证据。实际生产清理配置未核查。见 `apps/api/app/services/retention.py:43`、`:148`、`apps/api/app/db/models/interaction.py:60`。 |
| 素材交付 | 观点筛选、字幕搜索、详情和证据接口 | PRD 已规划 export/webhook，但本次核对的路由中未确认实现；未见统一素材包、交付版本、撤回/更正与采用反馈。见 `01_finance_opinion_radar_PRD.md:1039`、`apps/api/app/api/v1/__init__.py:14`。 |
| 跨源归并 | 同视频相似观点合并、作者主题历史与共识 | 已有去重按同来源实体/方向/字符相似度处理，未比较期限和条件；不能直接当作跨平台共同原始出处识别。见 `apps/api/app/services/entity_normalizer.py:93`。 |

### 建议补齐的六项产品能力

1. **可验收的标注规范与基准**：由内容研究员真正需要的检索、比较和引用标签倒推定义；明确正反例、不适用、未提及和无法判断。先补足现有 PRD 的人工样本要求，再按真实来源和难例持续扩充，保留独立验收样本；数量本身不证明质量。
2. **保留原始内容的清洗与上下文处理**：原文和清洗结果分开保留并可定位；重点识别数字、单位、实体、否定、条件、说话人及引用关系。按实际来源类型逐步增加处理规则，避免一次建设所有格式能力。
3. **有证据的语义核验和放行**：检查对象、方向、期限、条件、归属与原文支持关系；拿不准可弃权或待复核，失败不可伪装成无内容。按已识别错误类型选择规则、模型复核或人工，不能以增加模型数量代替准确性证明。
4. **人工纠错回流**：让审核人员在同一处看到标签、原话及上下文；修正记录沉淀成困难样本，并按转写、抽取、归属、合并错误分类。修正后的新版本及失效状态需要通知已取用的下游。
5. **可长期引用的证据资产**：保留来源链接、原文、定位、必要上下文、条件期限及审核版本。内容中心已引用的证据应有独立保留规则；不能只依赖精华手工标记或只剩结论的摘要。
6. **面向内容中心的最小素材交付**：复用检索，交付主题/实体、陈述类型、原话与归纳、来源定位、条件期限、核验状态和版本；接收采用、退回、错误原因。先交付可靠单条素材，再按需要做跨源事件归并，并区分转载与独立来源；最终选题与成稿仍由内容中心负责。

“准确抽出原话”和“原话所述事实已经核实”必须分开。先标事实性陈述、观点、预测、转述、方法等类型；未经外部核实的事实性陈述不得自动变成已证实事实。多个作者重复同一说法也不能当作独立证据。

### 建议顺序与三个月验收

| 阶段 | 最小交付 |
|---|---|
| 第一个月 | 选定实际常用素材范围及标签；补人工标注基准，统一评测口径；完成原始证据保留规则。 |
| 第二个月 | 针对基准暴露的错误完善清洗、归属、条件/期限、语义核验和人工复核；修正纳入样本与版本。 |
| 第三个月 | 向内容中心交付可回看、可引用的素材；跑通采用/退回/更正反馈，按真实使用结果验收。 |

日历是建议顺序，阶段推进应以质量结果为准，不能因到了月份就放行。以上是产品建议，不是已批准开发计划。

- 验收对象：实际交付给内容中心的素材，按来源/标签/难例分层人工核验，记录分母与样本量。
- 核心指标：被放行素材的字段正确率与原文支持率；原文证据可回看率；漏标/弃权比例；人工修改率；内容中心采用率及退回原因。
- 交付规则：缺必要来源证据或尚待核实的关键标签不得作为确认素材自动交付；不以删除所有困难样本来换取漂亮正确率。
- 当前线上质量尚无本次实测结果，不承诺百分之百准确。先对齐业务错误容忍度，再定自动交付与人工放行门槛。
- 跨行业扩展建议保留通用采集/清洗/证据能力，之后逐行业补标签定义、实体词表和已验证样例；暂不安排通用平台重构。

参考：[OpenAI 评估实践](https://developers.openai.com/api/docs/guides/evaluation-best-practices)建议使用贴近实际分布的数据与人工反馈校准自动评测；[结构化输出说明](https://openai.com/index/introducing-structured-outputs-in-the-api/)明确结构正确不保证字段内容正确。

## 2026-09-22 首批提案准备

- 用户已授权继续下一步，本轮将第一批收敛到标注质量基线，先生成规范；尚未获得产出后的 proposal 确认，不执行 apply。
- 本机 OpenSpec 1.2.0 已安装，原项目无 openspec 目录；已用 `openspec init --tools codex` 初始化。当前会话无 slash 入口，读取生成的 `.codex/skills/openspec-propose/SKILL.md` 并通过 CLI 完成提案流程。
- 变更：`establish-finance-label-quality-baseline`，单一新能力 `finance-label-quality-baseline`；proposal、design、spec 和 tasks 已写入对应 change 目录。
- 补充确认：现有评测固定 v1 prompt；生产抽取默认 v2 全文、超长回落 v1；评测无库路径只取前 50 段，且有库路径可能换成数据库中的转录。因此基准输入与处理路径需要显式冻结和记录。
- 最小实施边界：复用本地评测脚本、样本和文档，离线评分不连业务库；人工裁决负责语义匹配，确定性检查负责结构、证据位置和计数；不新增测试代码。
- 建议新增语义精确率至少 98% 作为首轮门槛，需随提案确认；沿用 PRD 的其他基准门槛。此数值是建议，不是当前准确率，也不代替人工审核覆盖和样本量要求。
- OpenSpec 严格校验已通过：1 项 passed、0 项 failed。该结果只证明规范格式有效，不表示代码已经实现或业务质量验收完成。
- 独立审阅已通过。审阅中发现并修正主状态/退出码的优先级及首次尝试与重试计数歧义；最终以预先计划的逻辑单元统计首次结构通过率，最多三次尝试，恢复后不抹去首次失败。
